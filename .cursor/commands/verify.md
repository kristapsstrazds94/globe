---
description: Check task acceptance criteria and DoD before marking done
---

Follow `.cursor/skills/definition-of-done/SKILL.md`.

If the user named a task ID, use that ID. Otherwise use the task currently **`IN PROGRESS`** in `TASKS.md`.

Read:

- `TASKS.md` (acceptance criteria for the ID)
- `AGENTS.md`
- Any docs the task required (per implement-globe-task doc table)

Report using the skill’s format. Do **not** set **`DONE`** unless the user has confirmed tests passed in this chat.

If criteria are unmet, list what remains. Do not start implementing unless the user asks to fix gaps.
